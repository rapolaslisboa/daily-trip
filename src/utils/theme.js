import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#238EA5",
    },
    secondary: {
      main: "#083B66",
    },
    background: {
      default: "#fff",
    },
  },
});

export { theme };
